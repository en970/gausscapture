[project]
name = "gausscapture"
version = "0.1.0"
description = "Local CapturePack to 3D Gaussian Splatting MVP pipeline"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

